const config = {
  FIREBASE: {
    API_KEY: process.env.FIREBASE_API_KEY,
    PROJECT_ID: process.env.FIREBASE_PROJECT_ID,
    SENDER_ID: process.env.FIREBASE_SENDER_ID,
    DATABASE_NAME: process.env.FIREBASE_DATABASE_NAME,
  },
};
export default config;
